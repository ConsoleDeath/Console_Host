module.exports = {
	name: 'laborant',
	description: 'Restart to Bot',
	execute(message, args) {
	message.react('♿');
  if (reaction.emoji.name === '♿') {
    add.role('🥼учёный')
  }
  }
};